{
  "rewrites": [
    {
      "source": "/api/scrapling",
      "destination": "/api/index.py"
    }
  ],
  "buildCommand": "npm run build && pip install scrapling[fetchers] --break-system-packages && python -m scrapling install"
}
