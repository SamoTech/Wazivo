# Scrapling kept for its fast HTTP Fetcher (non-JS pages)
# Jina Reader API handles all browser-rendered pages (LinkedIn etc.)
# No Playwright browser binary needed — Jina runs the browser remotely
scrapling
