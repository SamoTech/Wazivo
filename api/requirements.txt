# Scrapling — adaptive web scraping framework
# https://github.com/D4Vinci/Scrapling
#
# [fetchers] installs Playwright + TLS fingerprint spoofing + browser automation
# This enables StealthyFetcher (bypasses Cloudflare, LinkedIn, etc.)
# and Fetcher (fast HTTP with Chrome TLS impersonation)
scrapling[fetchers]
